{
  "version": "1.0.0",
  "name": "asp.net",
  "private": true,
  "scripts": {
    "build": "tsc --build && copyfiles **/*.html ../wwwroot && copyfiles **/*.css ../wwwroot",

    "copyComic": "copyfiles **/*.png ../wwwroot"

  },
  "devDependencies": {
    "typescript": "5.3.3",
    "copyfiles": "2.4.1"
  }
}